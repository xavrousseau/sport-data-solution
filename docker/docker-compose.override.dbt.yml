# ====================================================================================
# FICHIER   : docker/docker-compose.override.dbt.yml
# OBJET     : Service DBT (modélisation SQL) — exécutions interactives & docs
# CONTEXTE  : S’appuie sur PostgreSQL interne ("sport-postgres") et variables du .env
# ====================================================================================

services:

  # -----------------------------------------------------------------------------
  # 🧱 DBT CLI — écrire/tester des modèles SQL, générer la doc, lancer des tests
  # -----------------------------------------------------------------------------
  sport-dbt:
    image: ghcr.io/dbt-labs/dbt-postgres:1.7.13   # version explicite, stable
    container_name: sport-dbt
    working_dir: /usr/app                          # racine du projet dbt dans le conteneur

    # On monte le projet et le profile au bon endroit (cohérent avec DBT_PROFILES_DIR)
    volumes:
      - ./dbt:/usr/app
      - ./dbt/profiles.yml:/usr/app/profiles.yml:ro

    environment:
      # DBT lira ce profiles.yml (évite /root/.dbt et aligne avec ton .env)
      DBT_PROFILES_DIR: /usr/app                  # ← cf. .env
      # Connexion Postgres (injectée depuis .env pour rester DRY)
      PGHOST: ${PGHOST}
      PGPORT: ${PGPORT}
      PGUSER: ${PGUSER}
      PGPASSWORD: ${PGPASSWORD}
      PGDATABASE: ${PGDATABASE}
      DBT_TARGET: ${DBT_TARGET}
      TZ: Europe/Paris

    # Service persistant pour pouvoir docker exec / docker compose run selon tes usages
    entrypoint: ["sleep", "infinity"]

    healthcheck:
      # Vérifie simplement que la CLI dbt répond (la connexion DB n’est pas requise ici)
      test: ["CMD-SHELL", "dbt --version || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

    depends_on:
      # Attend Postgres "healthy" pour éviter les messages d’erreur au premier run
      sport-postgres:
        condition: service_healthy

    restart: unless-stopped
    networks:
      - sport-network   # même réseau que le reste de la stack

# Réutilise le réseau commun déjà défini dans le compose principal
networks:
  sport-network:
    driver: bridge
