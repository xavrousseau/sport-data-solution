# ==========================================================================================
# Fichier     : docker/ntfy/ntfy.yml
# Objectif    : Configuration du serveur ntfy en mode broker pour accepter les POST/PUT
# Auteur      : Xavier Rousseau — Juillet 2025
# ==========================================================================================

# URL de base (requis si ntfy est derrière un proxy ou appelé depuis scripts Python)
base-url: http://localhost:8080

# Port HTTP écouté dans le conteneur (déjà redirigé par docker-compose)
listen-http: ":80"

# 🔒 (Optionnel) Tu peux plus tard définir ici :
#   - authentication
#   - stockage des messages
#   - WebPush (notifications push)
#   - quotas, topics privés, expiration, etc.

# Documentation : https://docs.ntfy.sh/config/
